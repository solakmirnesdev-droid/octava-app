node_modules/
.nuxt/
.output/
.data/
dist/
# The Capacitor prep left `dist` as a symlink to .output/public, and a trailing
# slash matches directories only — so git kept offering to commit a link
# pointing at an absolute path on one machine.
dist
.env
.env.local
.DS_Store
*.log
